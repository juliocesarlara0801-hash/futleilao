services:
  - type: web
    name: futleilao
    runtime: node
    plan: free
    buildCommand: npm run install:all && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.12.0
      - key: ANTHROPIC_API_KEY
        sync: false
