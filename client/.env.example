# URL do servidor (Socket.IO/API). Deixe em branco em dev — o Vite já faz proxy
# de /socket.io e /api para http://localhost:3001 (ver client/vite.config.ts).
# Preencha em produção se o client for servido separado do server, ex:
# VITE_SERVER_URL=https://seu-servidor.exemplo.com
VITE_SERVER_URL=
